import 'package:flutter/material.dart';

class WaredrobePage extends StatelessWidget {
  const WaredrobePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Waredrobe Page'),
      ),
    );
  }
}